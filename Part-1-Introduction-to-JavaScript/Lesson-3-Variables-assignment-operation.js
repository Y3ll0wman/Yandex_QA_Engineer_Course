/*
Объяви константу car, переменные bus и carSharing так, чтобы:
в car содержалось значение 'Mercedes',
в bus — 'Volvo',
а переменная carSharing содержала значение из car.
*/

const car = 'Mercedes',
let bus = 'Volvo';
let carSharing = car;

// Выведи в консоль значения переменных car, bus и carSharing.

const car = 'Mercedes';
let bus = 'Volvo';
let carSharing = car;
console.log(car,bus,carSharing);