/*
У машины турбированный движок мощностью 249 лошадиных сил. Максимальная скорость — 260 км/ч. Объяви переменные engine, turbo, maxSpeed. Присвой им значения '249 hp', true , 260 соответственно.
Выведи значения переменных и их типы.
*/

let engine = '249 hp';
let turbo = true;
let maxSpeed = 260;
// выведи переменные engine, turbo и maxSpeed
console.log(engine, turbo, maxSpeed);
// выведи типы переменных engine, turbo и maxSpeed
console.log(typeof engine,typeof turbo,typeof maxSpeed);